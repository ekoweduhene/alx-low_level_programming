#include <stdlib.h>
#include <stdio.h>
/**
 * main- function that prints its name
 * @argc: stores arguments passed
 * @argv: pointer
 * Return: 0
*/
int main(int __attribute__((unused)) argc, char *argv[])
{
	int i = 0;

	printf("%s\n", argv[i]);
	return (0);
}
